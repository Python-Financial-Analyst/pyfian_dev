[project]
name = "pyfian-dev"
version = "0.1.0"
description = "Package for Python Financial Analyst"
authors = [
    {name = "Pablo Orazi",email = "pabloorazi@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
